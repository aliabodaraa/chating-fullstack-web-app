export type TypeContacts = { id: string; name: string }[];
export type TypeHandleSubmit = (e: React.FormEvent<HTMLFormElement>) => void;
export type setRefToLastMessageFunType = (node: HTMLDivElement) => void;
